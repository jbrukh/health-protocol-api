# Database connection string (Railway provides this automatically)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/health_protocol

# API key for authentication
API_KEY=your-secret-api-key-here

# Environment (development, production)
ENVIRONMENT=development

# Optional: Server URL for OpenAPI schema (auto-detected on Railway)
# SERVER_URL=https://your-app.railway.app
